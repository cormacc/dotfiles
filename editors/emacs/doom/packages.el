;; -*- no-byte-compile: t; -*-
;;; $DOOMDIR/packages.el

;; Intentionally empty. This config declares no extra packages and no
;; `package!' pins, by design -- see the change-record at
;; design/log/2026-09-02-replace-hand-rolled-doom-emacs-wiring-wi.org.
;; Doom's own modules manage their own package pins; adding pins here would
;; only add to the upstream pin-translation surface that
;; nix-doom-emacs-unstraightened's README warns "will break at times".
