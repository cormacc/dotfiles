#setxkbmap -model pc104 -layout ie -option caps:escape -option terminate:ctrl_alt_bksp
#Don't set layout options here -- otherwise dock-specific layout gets overridden every time we launch a shell
# setxkbmap -option caps:swapescape -option terminate:ctrl_alt_bksp
