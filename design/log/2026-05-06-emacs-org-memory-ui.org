#+TITLE: Emacs-based UI for org-memory extension
#+DATE: 2026-05-06 Wed
#+PARENT_ID: 9b5067ca-fec2-4e23-b647-981c31ee9a87
#+STATUS: Accepted
#+TODO: TODO(t) STARTED(s) WAITING(w) | DONE(d) CANCELLED(c)

* Context

The pi =tasks= extension (see =agents/pi/extensions/tasks/README.md=) is the
canonical UI today over the plain-org task-memory protocol owned by the
=org-tasks= / =org-plan= / =org-jira= skills. It provides a centred split-pane
tree+details view, status cycling, single-selection via the checkout-local
=#+SELECTED:= pointer in =TASKS.local.org=, change-record open/create via
=#+IMPORT: [[file:...]]= links (=p=), archive (=A=), publish/unpublish
(=P=/=U=), linked-issue badges + browser open (=J=), doctor checks, slash
commands, leader-menu keybindings, and a compact selected-task widget.

Emacs already has a thin minor-mode layer at
=editors/emacs/spacemacs/layers/org-user/local/tasks-org/tasks-org.el= that
covers some file-level helpers: =:ID:= ensure, =#+SELECTED:= toggle,
open existing =#+IMPORT:= change-records, publish/unpublish local tasks,
and jump-to-parent. Those helpers piggy-back on ordinary org-mode editing
and are useful prior art, but they are not a stable compatibility surface:
selection writing is currently too destructive for local drafts, there is
no Emacs-side plan scaffold helper, and status transitions only cover part
of the org-memory lifecycle. Since there is little legacy use to preserve,
this UI work may replace or substantially refactor the existing helpers
where that produces a cleaner implementation.

This change-record plans a *visualisation/UI layer* added to the
existing =tasks-org= package, with =vui.el= (https://github.com/d12frosted/vui.el)
as the candidate rendering library. The goal is feature-parity with
the pi tasks extension's expanded UI for read/navigate/cycle workflows
while delegating durable state mutation to a refactored =tasks-org=
protocol helper surface and plain org editing where they fit.

** Design decisions

- Fold the UI into =tasks-org=, do not create a sibling package ::
  Considered keeping the new UI orthogonal as a separate package that
  depends on =tasks-org=, and rejected. The two pieces share the same
  domain (the org-memory protocol on the Emacs side) and should share
  one graph reader plus one set of protocol writers. A single package
  also mirrors the pi-side decision in the package-boundary plan
  (=2026-05-06-package-org-memory-protocol-extensions=) to bundle
  =tasks= with =emacsclient= rather than keep them split, and keeps
  the future Nix packaging slice clean (one Emacs deliverable next to
  the pi extensions). Concretely: the existing =tasks-org-mode= minor
  mode (file helpers in any org task buffer) and a new
  =tasks-org-ui-mode= derived major mode (dedicated UI buffer)
  cohabit under the existing namespace; both modules import a shared
  internal graph reader.
- Replace helper internals where needed, keep only a small protocol API ::
  Current =tasks-org= helpers are allowed to change or disappear if they are
  a poor fit. The stable boundary should become protocol-level functions
  used by both the minor mode and UI: load graph, locate task by UUID/source,
  read/write =#+SELECTED:= without dropping local drafts, open/create
  =#+IMPORT:= change-records, and transition status with org-memory
  LOGBOOK / CLOSED / =:STARTED:= semantics. The UI module must call those
  helpers rather than duplicating parser/writer code, but no legacy helper
  name is sacred.
- Match the pi tasks extension's UX semantics, not its rendering ::
  Key bindings, status cycling, selection behaviour, default collapse
  rules, and the split-pane shape should mirror
  =agents/pi/extensions/tasks/README.md= so muscle memory transfers.
  The rendering technology is Emacs-idiomatic (vui.el / overlays /
  buttons), not a port of the pi widget tree.
- Evaluate vui.el before committing :: =vui.el= is the leading
  candidate because it targets exactly this kind of "rich read-mostly
  buffer with navigable interactive regions" need that the pi tasks
  UI fills. The evaluation task below decides whether vui.el covers
  per-region keymaps, status-cycle hooks, badge/tag rendering, split
  panes (or whether splits are layered on top with =display-buffer=),
  and refresh-on-file-change cleanly. Fallback is overlays + a custom
  =special-mode= derivative.
- Read-mostly v1 with a minimal mutation substrate :: First cut focuses on
  navigate / expand / select / cycle and plan/edit jumps. Unlike the
  existing helper set, v1 may add a minimal Emacs-side =#+IMPORT:= scaffold
  helper so =p= can create an empty change-record skeleton; agent-assisted
  plan drafting remains deferred. Archive (=A=), publish/unpublish (=P=/=U=),
  agent-assisted plan expansion, task creation (=n=/=N=), and the doctor
  command are deferred to follow-up tasks unless they fall out cheaply from
  the chosen rendering layer.
- Files remain canonical :: As with =tasks-org=, the UI saves through
  ordinary org file writes. Pi's file-watcher refreshes the pi widget;
  Emacs refreshes via =auto-revert= on the buffers the UI is reading.
  No Emacs↔pi RPC.
- Emacs writes are co-canonical with pi-side writes for v1, not primary ::
  Considered making =tasks-org= the canonical writer with the pi extension
  delegating mutations through =emacsclient= / =emacs_eval=, retaining
  pi-side writers as a non-Emacs-user fallback. Rejected for v1.
  Rationale:
  - The v1 UI is read-mostly. Mutation parity with the pi extension
    is itself a follow-up workstream. Inverting canonical ownership
    of operations the UI doesn't yet implement is premature.
  - Pi-side parser/writer drift is hypothetical, not observed; the
    pi extension has comprehensive parser/lifecycle/scaffold/doctor
    test suites. Flipping pre-emptively spends complexity on an
    unverified problem.
  - Even with a fallback, a flip *adds* an Emacs-delegation layer on
    top of pi's existing writers — it does not remove the pi writers,
    so the surface to keep aligned grows rather than shrinks.
  - A safer staged path exists: if drift or org-mode hook-ecosystem
    needs become real, flip *just status transitions* first
    (the strongest single case — =org-todo= owns LOGBOOK / CLOSED /
    =:STARTED:= exactly) and measure before expanding.
  Triggers that would justify revisiting (whole-flip or partial):
  - An observed parser-drift bug producing non-round-trip-stable files
    between pi-side writes and Emacs reads (or vice versa).
  - Demand for org-mode lifecycle integrations the pi-side writer
    cannot honour (custom =#+TODO:= sequences,
    =org-after-todo-state-change-hook= consumers, agenda inclusion
    rules, =org-attach= flows).
  - Latency tolerance test: the operations targeted for delegation
    must complete via =emacsclient= within the latency budget that
    keeps the pi UI feeling instant (rule of thumb: <100ms p95 round
    trip on a warm local Emacs server).
  v2 candidate scope: status-transition delegation only, recorded
  under follow-ups; whole-flip is not on the roadmap unless the
  partial flip proves the model.
- Spacemacs binding scheme :: Two complementary launchers, both registered
  through the standard Spacemacs declarations:
  - Global launcher =SPC a t= (applications → tasks) so the UI is reachable
    from any buffer. Uses =spacemacs/declare-prefix= + =spacemacs/set-leader-keys=.
  - In-context alias =, ; T= added to the existing
    =spacemacs/set-leader-keys-for-major-mode 'org-mode= block in
    =editors/emacs/spacemacs/layers/org-user/packages.el= alongside the
    current =;s=, =;p=, =;P=, =;t=, =;Lp=, =;Lu= bindings, so users
    inside =TASKS.org= or a change-record reach the UI from the same
    prefix as the rest of =tasks-org=.
- UI buffer is an evilified Spacemacs buffer :: If =vui.el= is adopted,
  =tasks-org-ui-mode= derives from =vui-mode= (which itself derives from
  =special-mode=); if the fallback renderer is used, it derives directly
  from =special-mode=. The mode is registered with =spacemacs/evilify-map=
  (or the =evilified-state-evilify-map= helper) so vim motion keys remap to
  UI actions while =SPC= remains the global leader. Do NOT bind =SPC= as a
  local toggle — it would shadow the leader. The UI buffer must also be
  excluded from =tasks-org-mode='s filename auto-enable so the minor mode
  does not run inside the visualisation buffer.
- Transparent interop with org source buffers :: Crossing between source
  org buffers and the UI must preserve location and stay live without a
  manual refresh:
  - Launching the UI from a =tasks-org-mode= buffer positions the cursor
    on the task at point.
  - The UI re-renders automatically when =TASKS.org=, =TASKS.local.org=,
    or any loaded change-record changes on disk (via =file-notify=
    watchers), preserving the cursor task by UUID across re-renders.
  - =e= jumps to the source heading using =pop-to-buffer-same-window=
    semantics that reuse an already-visible source window rather than
    splitting again.
  - Graph load proactively calls =org-id-update-id-locations= over the
    loaded fileset so UUID jumps work without the user having opened
    each file manually.
- Project root via projectile :: Same convention as =tasks-org=:
  =projectile-project-root= with a directory fallback. The UI loads
  the graph rooted at =TASKS.org= + linked change-records + =TASKS.local.org=.
- ECA interop is loose and file/cursor-mediated for v1 :: ECA already
  consumes editor file path + cursor position as implicit context, so
  pressing =e= in the UI to land on a task heading in its source buffer
  is sufficient passive interop — no UI ↔ ECA RPC, no graph
  serialisation. An explicit ECA bridge (e.g. a =c= key that opens a
  chat scoped to the cursor task's UUID, summary, change-record path,
  and =:LINKED_ISSUES:=) is a v2 candidate, recorded under follow-ups.
  Avoid duplicating the pi-side =tasks_insert_task= cross-extension
  primitive; if mutation-via-agent becomes desirable later, route it
  through the same files rather than a parallel API.

* Plan

** TODO [#B] Evaluate vui.el for the UI implementation :research:
:PROPERTIES:
:ID: 367d217c-2355-42fa-87b4-bd6a035ba872
:CREATED: [2026-05-06 Wed 21:56]
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- A note (under =design/notes/= or appended to this change-record's
  =* Implementation= section) summarises:
  - How vui.el models interactive regions / per-region keymaps.
  - Whether it supports a split layout (tree + details) or whether
    that has to be built with =display-buffer= rules around plain
    vui buffers.
  - How it handles refresh / re-render when the underlying file
    changes on disk.
  - Whether status colours and badge/tag styling can be expressed
    without falling back to raw overlays.
  - Any hard blockers (e.g. fontification model incompatible with
    org).
- A go/no-go recommendation: adopt vui.el, adopt vui.el for parts
  only, or fall back to a hand-rolled =special-mode= + overlays
  approach.
- A short list of vui.el primitives the v1 UI will rely on, so later
  tasks can reference them concretely.
- Packaging/version note: whether to add =vui= to =org-user-packages= as an
  ELPA/MELPA package or file recipe, and whether its Emacs 29.1 requirement
  is acceptable for the current =pkgs.emacs-pgtk= setup.

Read =vui.el= upstream and build a small spike before committing
later tasks to its primitives.
** TODO [#B] Rebuild the =tasks-org= protocol helper surface :helpers:
:PROPERTIES:
:ID: 7fe12dc9-368e-42ff-ab9c-d7dd4a947697
:CREATED: [2026-05-06 Wed 23:06]
:BLOCKED-BY: 367d217c-2355-42fa-87b4-bd6a035ba872
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 23:06]
:END:
Acceptance criteria:
- Existing helper names and behaviours are reviewed against the current
  org-memory protocol and either retained, renamed, or discarded; no legacy
  API compatibility is required beyond commands documented in this plan.
- Selection writer preserves all non-selection content in =TASKS.local.org=:
  it updates or prepends exactly one =#+SELECTED:= line atomically while
  retaining local task headings and =#+IMPORT:= keywords.
- Plan helpers operate on =#+IMPORT:=, not =:PLAN:=. They can open an existing
  change-record from a UI task locator and can scaffold a new empty
  change-record skeleton after a user-confirmed path, using
  =#+DEFAULT_PLAN_DIR:= for suggestions. No agent prompt is part of v1.
- Status transition helper can cycle both directions from a task locator and
  writes protocol-compatible status state: heading keyword, first =:STARTED:=,
  current =CLOSED:= on DONE/CANCELLED, cleared =CLOSED:= on reopen, and
  append-only =:LOGBOOK:= entries. Use =org-todo= only if tests prove its
  configured output matches the protocol exactly; otherwise write the minimal
  protocol state explicitly.
- Locate/edit helpers let the UI act on a task by UUID/source file+line without
  assuming point is already in an org task buffer.
- Unit tests cover selection preservation with local drafts, =#+IMPORT:= open
  + scaffold insertion, and representative status transitions including
  reopen from DONE.
** TODO [#B] Define the in-Emacs task-graph model :model:
:PROPERTIES:
:ID: ee133a81-c08c-4820-bad4-e672f102d0a7
:CREATED: [2026-05-06 Wed 21:56]
:BLOCKED-BY: 367d217c-2355-42fa-87b4-bd6a035ba872
:BLOCKED-BY+: 7fe12dc9-368e-42ff-ab9c-d7dd4a947697
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- A documented data shape for a loaded task graph: top-level tasks,
  subtasks (including subtasks pulled in via =#+IMPORT:= change-record files),
  status, priority cookie, =:ID:=, =:CREATED:= / =:STARTED:= /
  =CLOSED:=, =:LINKED_ISSUES:=, tags, =#+IMPORT:= path/raw value,
  source file + line, and local-vs-shared origin.
- Graph reader lives inside =tasks-org= (per the fold decision in
  =* Context= → Design decisions): a pure-elisp reader built on
  =org-element-parse-buffer= for the open buffer plus
  =find-file-noselect= for imported files. Replace any existing ad-hoc
  traversal with this reader so there is one walker, not two.
- Reader lives in its own file (e.g. =tasks-org-graph.el=) loaded by
  =tasks-org.el= so the minor-mode entry point stays small.
- Public surface: a single =tasks-org-load-graph= function returning
  the documented data shape, plus narrow accessors / locators. The UI buffer
  is the only consumer for now; mutation helpers may still operate directly
  on source buffers, but they take graph locators rather than requiring point
  to already be on a task heading.
- Local-task ingestion: =TASKS.local.org= contributes both
  =#+SELECTED:= and any local-only task headings, separated visually
  in the UI as the pi extension does.
- Reload semantics: a single =tasks-org-ui-refresh= command rebuilds
  the graph; auto-refresh hooks deferred to a later task.
- Unit tests under the layer's =tasks-org-tests.el= cover at least:
  parsing =TASKS.org= with one =#+IMPORT:= change-record, parsing
  =TASKS.local.org= selection + local-task mix, linked issue extraction, and
  confirming graph reads do not mutate unrelated drawer properties.

The vui.el evaluation may constrain how the model is exposed to the
renderer, hence the dependency.
** TODO [#B] Implement the expanded tasks UI buffer :ui:
:PROPERTIES:
:ID: b44c8e53-7b12-4800-aab1-9319225356ec
:CREATED: [2026-05-06 Wed 21:56]
:BLOCKED-BY: 367d217c-2355-42fa-87b4-bd6a035ba872
:BLOCKED-BY+: ee133a81-c08c-4820-bad4-e672f102d0a7
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- New command =tasks-org-ui-show= (working title) opens a dedicated
  buffer, e.g. =*tasks-org*=, in a new derived major mode
  =tasks-org-ui-mode= (sibling module to =tasks-org-mode=, both
  living in the folded =tasks-org= package). With =vui.el=, the mode
  derives from =vui-mode= so =vui-mount= preserves the custom keymap;
  with the fallback renderer, it derives from =special-mode=. It is
  registered with =spacemacs/evilify-map= (or equivalent helper) so the
  buffer behaves like other Spacemacs read-only UIs (magit, ibuffer).
- The UI buffer must be excluded from =tasks-org-mode='s filename
  auto-enable hook, so the minor mode does not trip on the
  visualisation buffer.
- Initial cursor: when invoked from a =tasks-org-mode= source buffer
  with point on a task, the UI opens with that task as the cursor row.
  Otherwise: selected task if any, else first top-level.
- Layout: a single-buffer tree view in v1 is acceptable if vui.el
  split panes are not cheap. The details pane (right side in the pi
  UI) may be deferred to a follow-up task; it is *not* required for
  v1 acceptance.
- Renders for each task: status token (with the pi colour palette —
  TODO/STARTED/WAITING/DONE/CANCELLED), priority cookie, summary,
  tags, linked-issue badges, =#+IMPORT:= path indicator.
- Default collapse mirrors the pi extension: top-level visible,
  subtrees collapsed except the path to the selected task; sibling
  subtrees of the selected path stay collapsed; DONE/CANCELLED
  subtrees stay collapsed unless they are on the selected path.
- In-buffer keybindings (final keymap confirmed during implementation;
  =SPC= deliberately *not* bound — it remains the Spacemacs leader):
  - =j=/=k= or =↓=/=↑= — move cursor down/up.
  - =RET= / =TAB= — toggle collapse on the cursor task.
  - =l=/=h= or =→=/=←= — cycle status forward/back, persisting via
    the protocol status helper from the helper-surface task.
  - =s= — toggle selection on the cursor task (delegates to the
    non-destructive =#+SELECTED:= helper).
  - =e= — visit the cursor task at point in its source file. Reuses
    an already-visible source window via =pop-to-buffer-same-window=
    rules rather than creating new splits, then positions point via
    =org-id-goto= or line jump.
  - =p= — open / create the cursor task's =#+IMPORT:= change-record,
    delegating to the protocol plan helper. Creation scaffolds only the
    empty change-record; agent-assisted drafting is deferred.
  - =g= — refresh the graph from disk.
  - =q= — quit the UI buffer (=quit-window=).
- Status cycling persists through the protocol status helper. If it uses
  =org-todo= internally, tests must prove LOGBOOK / CLOSED / =:STARTED:=
  semantics match the org-memory protocol.
- Launchers (per the binding scheme in =* Context= → Design decisions):
  - =SPC a t= globally via =spacemacs/declare-prefix= +
    =spacemacs/set-leader-keys=.
  - =, ; T= added to the existing =set-leader-keys-for-major-mode 'org-mode=
    block in =org-user/packages.el= so it sits alongside the current
    =;s=, =;p=, =;P=, =;t=, =;Lp=, =;Lu= bindings.
- Live refresh: the UI registers =file-notify= watchers on =TASKS.org=,
  =TASKS.local.org=, and every loaded change-record. External edits
  (Emacs in another window, pi, ECA, hand-edits) trigger a
  re-render that preserves the cursor task by UUID where possible
  and falls back to first top-level when the cursor task disappears.
- ID jump prerequisite: the graph load runs =org-id-update-id-locations=
  over the loaded fileset so =org-id-goto= works on first =e= without
  the user having opened each file manually.
** TODO [#C] Compact selected-task indicator :ui:selection:
:PROPERTIES:
:ID: 0513b800-e184-4b24-a0ef-6dac18fca226
:CREATED: [2026-05-06 Wed 21:56]
:BLOCKED-BY: b44c8e53-7b12-4800-aab1-9319225356ec
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- A command =tasks-org-ui-show-selected= opens the compact view in a
  side window via =display-buffer= rules.
- Shows the containing top-level tree of the currently selected task,
  with the selected row marked (e.g. =★= prefix), at most 6 lines,
  with completed subtasks elided as =… N completed subtasks= when
  truncated.
- Refreshes when =TASKS.org= / =TASKS.local.org= / linked change-records
  change on disk (=auto-revert= or a small file-notify watcher).
- Hidden when the expanded UI buffer is current; restored otherwise.
- Documented as optional in the README — not required to use the
  package.

Optional Emacs analogue of the pi compact widget.
** TODO [#C] Linked-issue badges and =J= keybinding :issues:
:PROPERTIES:
:ID: d63cc5a5-57ee-4329-9ca7-07cc0800d162
:CREATED: [2026-05-06 Wed 21:56]
:BLOCKED-BY: b44c8e53-7b12-4800-aab1-9319225356ec
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- Each task row renders linked-issue tokens as =⤴= badges immediately
  before the tag suffix.
- Bare keys are resolved against =#+ISSUE_URL_BASE= using the same
  rules documented in the tasks extension README (=`{ID}`= placeholder
  or prefix-append; URL-encode the key).
- Org-link tokens (=[[url][label]]=) render as the label.
- =J= on a task opens every resolvable URL via =browse-url= (which
  picks =open= / =xdg-open= per platform). Cap at 5 with a message
  when exceeded; silent no-op when the property is empty.
- Bare tokens with no resolvable URL trigger a =message= pointing at
  the missing =#+ISSUE_URL_BASE= keyword.

Mirrors the pi extension's =:LINKED_ISSUES:= rendering and browser
open key.
** TODO [#C] Document the UI in the layer README :docs:
:PROPERTIES:
:ID: 2990f011-7534-4010-8f9f-e3e89d645937
:CREATED: [2026-05-06 Wed 21:56]
:BLOCKED-BY: b44c8e53-7b12-4800-aab1-9319225356ec
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- README documents activation, the keymap, status colour palette, and
  default collapse rules.
- README states explicitly which pi-extension features are NOT yet
  ported (archive =A=, publish/unpublish =P=/=U=, doctor,
  agent-assisted plan drafting) and links to the corresponding
  follow-up tasks.
- README cross-references =agents/pi/extensions/tasks/README.md= as
  the canonical UX spec the Emacs UI mirrors.
- README lists the file-protocol contracts inherited from
  =tasks-org=: =:ID:=, =#+IMPORT:=, =#+SELECTED:=,
  =#+ISSUE_URL_BASE=, =#+DEFAULT_PLAN_DIR=.

Update =editors/emacs/spacemacs/layers/org-user/README.org= so the
new UI is discoverable and its scope vs the pi extension is clear.
** TODO [#D] Validate end-to-end against TASKS.org and a real plan :verification:
:PROPERTIES:
:ID: 9a985abf-0572-43dc-8df3-5fde75567a17
:CREATED: [2026-05-06 Wed 21:56]
:BLOCKED-BY: b44c8e53-7b12-4800-aab1-9319225356ec
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- Open this repo's =TASKS.org=, launch the UI, navigate to a task
  with a =#+IMPORT:= change-record, expand into subtasks, cycle status, and confirm:
  - The on-disk file shows the new status with LOGBOOK + CLOSED
    semantics from the protocol status helper.
  - The pi tasks extension picks up the change via its file watcher
    without requiring a reload.
- Toggle =#+SELECTED:= via =s= and confirm the pi compact widget
  shifts selection accordingly without dropping any local draft content.
- Press =p= on a task with no =#+IMPORT:= and confirm the protocol plan
  helper's scaffold path runs end-to-end through the new UI.
- Record any divergences from the pi UX as new TODOs in =TASKS.org=
  rather than expanding v1 scope.

Manual smoke test mirroring the =tasks-org= validation task
(=437d6716-1135-4459-ad0f-ee9f88eff294=) — but for the UI buffer.
** TODO [#D] File follow-ups deferred from v1 :followup:
:PROPERTIES:
:ID: d19bfbf6-a161-41a0-8779-c4f79d3b289f
:CREATED: [2026-05-06 Wed 21:56]
:BLOCKED-BY: 9a985abf-0572-43dc-8df3-5fde75567a17
:END:
:LOGBOOK:
- Created [2026-05-06 Wed 21:56]
:END:
Acceptance criteria:
- Each deferred parity gap is filed as its own =TODO= in =TASKS.org=
  when v1 closes, referencing this change-record. At minimum:
  - Archive flow (pi =A=).
  - Publish / unpublish flows (pi =P= / =U=).
  - Doctor command (pi =/tasks doctor=).
  - Agent-assisted plan drafting (pi =p= on tasks with no =#+IMPORT:=
    triggers an agent prompt after scaffolding — Emacs analogue likely needs
    an =eca=/=pi=-side bridge).
  - Active ECA bridge: candidate =c= keybinding that opens an ECA chat
    pre-injected with the cursor task's UUID, summary, =#+IMPORT:= path,
    and =:LINKED_ISSUES:= as context. Counterpart to the pi =p=
    agent-assisted flow. v1 is intentionally cursor/file-mediated only;
    this is the active surface deferred per the ECA-interop design
    decision.
  - Emacs-primary status-transition delegation (partial flip): pi
    extension delegates =org-todo= calls to Emacs via =emacsclient= /
    =emacs_eval= when an Emacs server is available, falling back to
    in-process pi writers otherwise. Scoped intentionally to the
    single strongest case identified in the v1 review — *not* a
    whole-flip. Pre-requisites: a stable Emacs-side RPC surface
    (named callable functions with documented signatures and
    defensive error handling) plus the latency tolerance test from
    the co-canonical Design decision. Triggers for picking this up
    are also documented there.
  - Details pane (right-hand pane in the pi UI), if vui.el split-pane
    support landed in v1 only as a tree view.
  - Task creation (pi =n= / =N=).
  - Live cross-process sync with pi (events, not just file watch).

Capture parity gaps as separate workstreams once v1 ships so they
don't expand v1 scope.
* Implementation

Implementation notes will be added as tasks are completed. Keep this
section focused on:
- vui.el evaluation findings and the concrete primitives chosen.
- Tricky =org-element= / status-writer integration details discovered
  during the UI build.
- Any divergences from the pi tasks extension's UX and why.

* Open questions

** OPEN Should the folded =tasks-org= package relocate out of the Spacemacs =org-user= layer?
The fold decision keeps the UI inside =tasks-org=, but =tasks-org=
currently lives at
=editors/emacs/spacemacs/layers/org-user/local/tasks-org/=. To be
packageable alongside the pi extensions under the dual-mode Nix
bundle (see parent task =79d6f4f5= and its plan
=2026-05-06-package-org-memory-protocol-extensions.org=), the
package may want to move to a top-level location such as
=agents/emacs/tasks-org/= and be loaded by the Spacemacs layer via
=:location (recipe :fetcher file ...)= instead of =:location local=.

Not blocking for v1 of the UI — but resolve before any packaging
work touches the Emacs side, otherwise the move turns into a
rename-with-history exercise mid-bundle.

** OPEN When does the partial flip (Emacs-primary status transitions) earn its keep?
v1 keeps Emacs writes co-canonical with pi-side writes, per the
Design decision. Revisit when at least one of the following is
documented:
- A reproducible parser-drift bug between pi-side writes and Emacs
  reads (or vice versa) that produces non-round-trip-stable files.
- A concrete user request blocked because the pi-side writer cannot
  honour an org-mode customisation (custom =#+TODO:= sequences,
  hook consumers, agenda rules, attachments).
- Latency-tolerance test data showing =emacsclient= roundtrip stays
  under 100ms p95 on the target hardware.
Decision rule: when ≥2 of the above are true, file a TODO scoped
specifically to status-transition delegation and the Emacs-side RPC
surface — not a whole-flip. Whole-flip is off-roadmap unless the
partial flip ships and validates the operational model.

** OPEN When does the active ECA bridge (=c= keybinding) earn its keep?
v1 deliberately stays cursor/file-mediated. Revisit once the UI is in
daily use: if users repeatedly do "press =e=, switch to ECA chat,
manually paste task UUID + change-record path", the active bridge
pulls its weight. If ECA's automatic cursor context already covers
the workflow, leave the bridge unbuilt. Decision criterion: count of
manual-paste-context observations during the v1 validation task and
the first month of use.

** OPEN Should the Emacs UI ever drive pi via an event bridge, or is file-watch sync sufficient long-term?
Answer expected during the v1 validation task. If file-watch latency
proves visible during interactive use, the follow-up workstream
should spec a small RPC or named-pipe bridge — but only after
confirming the problem is real.

** OPEN Single buffer vs split tree+details for v1?
Default plan is single-buffer tree in v1 with the details pane as a
follow-up. Revisit during the vui.el evaluation: if vui makes the
split trivial, ship it in v1.

** OPEN Should status cycling go through =org-todo= or a custom writer?
Candidate default is =org-todo= because it keeps Emacs users close to normal
org workflows, but the helper-surface task must prove the emitted LOGBOOK /
CLOSED / =:STARTED:= state matches the org-memory protocol. If the configured
=org-todo= behaviour diverges or prompts intrusively, write the minimal
protocol state explicitly instead.
