#+TITLE: Things wot I should do
#+AUTHOR: Cormac Cannon
#+LANGUAGE: en
#+DATE: 2026-04-25
#+TODO: TODO(t) STARTED(s) WAITING(w) | DONE(d) CANCELLED(c)
#+DEFAULT_PLAN_DIR: [[file:./design/log]]

# Tracker linkage (consumed by the `tasks` extension for badge rendering
# and `J` browser-open of bare keys in `:LINKED_ISSUES:`).
#+ISSUE_URL_BASE: https://your-org.atlassian.net/browse/{ID}

# Jira-specific keywords (consumed by the `jira` extension only).
#+JIRA_CLOUDID: 00000000-0000-4000-8000-000000000000
#+JIRA_PROJECT:
#+JIRA_BASE_URL: https://your-org.atlassian.net


# EXPORT SETUP

# 1. Ensure exports include both source block contents and any results
#+PROPERTY: header-args :exports both

# 2. Suppress author in title, and prevent ^ and _ from initiating super and subscript....
#     N.B. can still use the braced forms -- i.e. 'bla^{super}_{sub}'
#+OPTIONS: author:nil ^:nil

# 3. HTML export theme
#+SETUPFILE: ~/Documents/templates/org-html-themes/org/theme-readtheorg.setup

# 4. ODT (and DOCX) export styles/theme
#    N.B. Editing the ODT can cause the embedded logo jpg name to change... if this occurs, unzip the odt to check the new name and update this line
#+ODT_STYLES_FILE: ("~/Documents/templates/nmd-template.odt" ("styles.xml" "content.xml" "Pictures/1000000000000A4F000004659ACFB30B1A81D55F.jpg"))


# Startup options : Want to view rendered versions of latex fragments (equations etc.) by default
#+STARTUP:: latexpreview


Some introductory text

* Table of Contents                                         :TOC_1_gh:noexport:
- [[#improvements][Improvements]]
- [[#fixes][Fixes]]
- [[#housekeeping][Housekeeping]]

* Improvements
** DONE [#C] Add some sort of Jira integration for the tasks extension
CLOSED: [2026-04-28 Tue 14:00]
:PROPERTIES:
:ID: 0276a6c7-0ca8-4c39-a90a-05587ad64698
:STARTED: [2026-04-28 Tue 13:27]
:END:
#+IMPORT: [[file:design/log/2026-04-28-jira-integration.org]]
*** TODO [#C] Setup Jira access
:PROPERTIES:
:ID: d618bcc1-9481-4431-a572-8a1f17612e4f
:END:
The atlassian mcp server has been setup - connect using /mcp reconnect atlassian.
Consider what should be implemented via a jira extension to
*** TODO [#A] Read-only Jira integration
:PROPERTIES:
:ID: 0d3431c4-d997-41ef-8e6d-0925a386ea6b
:END:
**** TODO [#A] Reference jira issues
:PROPERTIES:
:ID: 097a6e4f-6752-4f97-8b41-f416f8b47384
:END:
Update the tasks extension and related skills to allow us to reference jira issue types.
The mapping is not one-to-one -- a single task here may link to one-to-many Jira issues.
The most ergonomic approach from a user perspective would be to just include issue IDS -- in the properties drawer?
And maybe embed a Jira URL and default project in TASKS.org header metadata.
**** TODO [#B] Clone Jira issues
:PROPERTIES:
:ID: edbea049-3a56-40ad-9aa5-660843364524
:END:
Pull an item from Jira and create a task here.
*** TODO [#B] Write/edit Jira integration
:PROPERTIES:
:ID: e7b91f96-ec83-4052-ab58-249e876bd946
:END:
**** TODO [#B] Update Jira issues
:PROPERTIES:
:ID: 6a808310-54c6-4ab6-ae47-8c8ef0bd6e55
:END:
Do things like...
- Claim task (update assignee)
- Add comments
- Transition through workflow
**** TODO [#C] Create Jira issues
:PROPERTIES:
:ID: cfc094b3-870c-4c93-833b-f5a05d478d4a
:END:
** TODO [#C] Token-efficient Jira clone workflow
:PROPERTIES:
:ID: 4ad4bb1b-9e74-48a0-9ce4-2d69639a8bc8
:CREATED: 2026-04-28 Tue 15:16
:END:
Follow-up to the read-only Jira integration. Today's `/jira clone` round-trips the
entire `atlassian_getJiraIssue` JSON and the rendered org body through the model,
which is wasteful (~17KB ingested for ~6 fields used). Step 1 (field-list filter
in the prompt) shipped on 2026-04-28. The remaining wins below are tracked here.

*** TODO [#C] Two-step clone with per-session cache
:PROPERTIES:
:ID: 5a0b8c2e-f1a3-4d24-b6e8-6c1c2e9b1a01
:END:
Replace the single all-in-one prompt with a fetch + apply pair:
- ~jira_clone_fetch(key)~ — extension-registered tool. Wraps the MCP
  ~atlassian_getJiraIssue~ call with the field-list filter, persists the
  filtered result in a per-session cache keyed by issue key, and returns a
  compact preview (summary, priority, label list, body length, ambiguities) to
  the model. The full body never enters the model context unless requested.
- ~jira_clone_apply(key, file, section, overrides?)~ — extension-registered
  tool. Reads the cached issue, performs deterministic transforms (priority
  mapping, UUID, ~CREATED~ timestamp, ~:LINKED_ISSUES:~ drawer, label tagging,
  body insertion), and writes the org file via the tasks-extension helper
  (see sibling task). Body text never appears in model-emitted tool args.
- Goal: ~70% token reduction on body-heavy issues. Model is only involved in
  judgement decisions (file/section placement, summary surgery, table cleanup
  on broken ADF→markdown round-trips).
- Open question: cache lifetime + invalidation. Probably session-scoped + an
  explicit ~jira_clone_refresh~ escape hatch.
- Architectural caveat: pi-mcp-adapter currently routes MCP through the LLM
  in proxy mode; extensions can't directly call MCP from JS. The fetch step
  therefore stays model-driven (single small tool call), but its *output* is
  captured into the cache by the registered tool wrapper rather than echoed
  through the conversation. Verify this is achievable with the current
  ExtensionAPI before committing to the design.

*** TODO [#C] Shared ~tasks.insertTask~ API
:PROPERTIES:
:ID: 6c2d4f8b-3a91-4e57-b04c-9d7d8a3a9b02
:END:
Factor the org-task rendering + file-insertion logic out of the (future) jira
clone path into a shared helper exposed by the tasks extension:
- Inputs: ~{ file, section, summary, priorityName, body, linkedIssues, labels,
  tags?, parentId? }~.
- Owns: priority mapping (Highest..Lowest → #A..#D), UUID generation, ~CREATED~
  timestamp via ~date +"%Y-%m-%d %a %H:%M"~, drawer assembly, section creation
  guard rails, idempotency on re-clone of the same key.
- Consumers: jira clone, future github/linear/gitlab integrations, and
  potentially ~/jira create~ on the reverse direction.
- Side benefit: removes the duplicated drawer/UUID/priority logic that's
  already drifting between the jira prompt template and the extension code.
- Acceptance: the jira ~clone_apply~ tool above implemented purely on top of
  this helper, with no org-mode string assembly inside the jira extension.

** TODO [#D] Package org-memory protocol / extensions
:PROPERTIES:
:ID: 79d6f4f5-70f9-404d-9b4c-0cf0ef0e7adb
:END:
Currently the skills and extensions are embedded in this repo.
Good to either extract them, or come up with a packaging/deployment strategy to facilitate sharing them with the team / other
interested parties.
Ideally the approach would allow me to continue editing skills/extensions live and reloading without having to go through a
rebuild install step.
* Fixes
** TODO [#B] Update keybindings extension to handle capital letter properly
:PROPERTIES:
:ID: d9789ed7-ff34-490a-a818-bf7a79fa1180
:END:
#+IMPORT: [[file:design/log/2026-04-27-keybindings-capital-letters.org]]
Currently a key sequence SPACE t A isn't activated as expected. The keybindings extension toggles on the sequence SPACE t SHIFT a.
* Housekeeping

** WAITING [#C] Update nixGL flake URL :nix:
:PROPERTIES:
:ID: 862e9ecf-6992-40e9-b2e2-597edee1ccfa
:BLOCKED-BY: url:https://github.com/nix-community/nixGL/pull/187
:END:
Waiting on an upstream fix for an nvidia-open issue
